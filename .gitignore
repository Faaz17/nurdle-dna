# Claude Code — local/private files (never commit these)
.claude/CLAUDE.local.md
.claude/settings.local.json
.claude/.mcp.json

# Website — React / Next.js
node_modules/
.next/
out/
dist/
build/
.env
.env.local
.env.*.local

# Autodesk Fusion 360 autosave / cache
*.f3d.autosave
Backup/

# Large binary 3D exports — uncomment once files are finalised
# *.fbx
# *.obj
# *.gltf
# *.glb

# Firebase credentials — fill in firebase-config.js locally
# The template (REPLACE_ME) is safe to commit; real creds must NOT be
jetson/serviceAccountKey.json
jetson/config_local.py

# Jetson model weights
jetson/models/

# Jetson Orin SD-card image (~11 GB; download from NVIDIA — see Jetson Start/README.md)
Jetson Start/*.zip
Jetson Start/*.img

# OS junk
.DS_Store
Thumbs.db
desktop.ini
