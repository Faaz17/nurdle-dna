# Never commit real credentials
serviceAccountKey.json
config_local.py
.env
__pycache__/
*.pyc

# Large training weights (the small ONNX deployment model IS committed
# explicitly via `git add -f` so `git clone` brings everything needed)
*.pt
*.engine
